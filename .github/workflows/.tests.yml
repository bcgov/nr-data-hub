name: Integration Tests

on: [pull_request]

jobs:
  get-schemas:
    name: Get Schemas
    description: Lists all the schemas ahead of further testing
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Set Matrix
        id: set-matrix
        run: echo "::set-output name=matrix::$(ls schemas/**/*.schema.json | jq -R -s -c 'split("\n")[:-1]')"

  validation:
    name: Validate Schemas
    description: Validates all schemas in the schemas directory
    runs-on: ubuntu-latest
    timeout-minutes: 1
    needs: list-schemas
    strategy:
      matrix:
        schemas: ${{ fromJson(needs.get-schemas.outputs.matrix) }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.13'

      - name: Install Packages
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
        
      - name: Validate Schemas
        run: echo ${{ matrix.schemas }}
